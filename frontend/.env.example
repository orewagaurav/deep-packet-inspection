# Base URL of the DPI backend API.
# Set this in your host (Vercel/Netlify) env vars, or in a local .env file.
# If unset, the app falls back to the current hosted backend.
VITE_API_URL=http://localhost:8000
